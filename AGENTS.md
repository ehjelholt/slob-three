# Agent Skills

## Issue tracker

Issues live in GitHub Issues (`ehjelholt/slob-three`). Use the `gh` CLI for all operations. See `docs/agents/issue-tracker.md`.

## Triage labels

The AFK-ready label is `sandcastle` (not `ready-for-agent`). See `docs/agents/triage-labels.md` for the full mapping.

## Domain docs

Single-context repo — one `CONTEXT.md` at the root and `docs/adr/` for architectural decisions. See `docs/agents/domain.md`.
